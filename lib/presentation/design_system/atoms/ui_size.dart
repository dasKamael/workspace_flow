/// Spacing scale of the design system — 4 / 6 / 8 / 10 / 12 / 14 / 16 / 20 / 26 / 32.
class UiSize {
  UiSize._();

  /// 4.0
  static const double xxs = 4.0;

  /// 6.0
  static const double xs = 6.0;

  /// 8.0
  static const double s = 8.0;

  /// 10.0
  static const double sm = 10.0;

  /// 12.0
  static const double m = 12.0;

  /// 14.0
  static const double ml = 14.0;

  /// 16.0
  static const double l = 16.0;

  /// 20.0
  static const double xl = 20.0;

  /// 26.0
  static const double xxl = 26.0;

  /// 32.0
  static const double xxxl = 32.0;

  //  ------ Aliases ------
  /// Height of the window title bar.
  static const double titleBarHeight = 46.0;

  /// Height of the window body.
  static const double windowBodyHeight = 736.0;

  /// Design width of the app window.
  static const double windowWidth = 1240.0;

  /// Diameter of a traffic-light dot.
  static const double trafficLightDot = 12.0;

  /// Diameter of the project status dot.
  static const double statusDot = 8.0;

  /// Edge length of a tick box / checkbox.
  static const double tickBox = 17.0;

  /// Width of the blocker switch.
  static const double switchWidth = 44.0;

  /// Height of the blocker switch.
  static const double switchHeight = 24.0;

  /// Diameter of the blocker switch knob.
  static const double switchKnob = 18.0;

  /// Edge length of the focus dial in the session card.
  static const double dial = 182.0;

  /// Edge length of the progress ring in the running session.
  static const double focusRing = 340.0;

  /// Width of the project editor sheet.
  static const double sheetWide = 900.0;

  /// Width of the profile editor sheet.
  static const double sheetNarrow = 600.0;

  /// Width of the blocked page window.
  static const double blockedPageWidth = 700.0;

  /// Height of the blocked page window.
  static const double blockedPageHeight = 340.0;

  /// Width of the left column in the project editor.
  static const double editorSourcesColumn = 268.0;

  /// Height of the progress bar under an opening app row.
  static const double progressBarHeight = 2.0;

  /// Width of the focus ring drawn around a focused input.
  static const double focusRingWidth = 3.0;
}
