// coverage:ignore-file
import 'package:flutter/painting.dart';

/// macOS window button colours, used by the title bar.
class TrafficLightColorPalette {
  TrafficLightColorPalette._();

  static const Color close = Color(0xFFFF5F57);
  static const Color minimize = Color(0xFFFEBC2E);
  static const Color zoom = Color(0xFF28C840);
}
