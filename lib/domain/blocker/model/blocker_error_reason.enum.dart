/// Why the blocker couldn't do what the UI just asked, or why enforcement that looks
/// armed silently isn't working for one browser.
enum BlockerErrorReason { armFailed, disarmFailed, sitePermissionDenied }
