name: workspace_flow
description: "Focus App for macOS — Projects, App Blocker, Focus Session."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5
  flutter: ^3.41.9

dependencies:
  flutter:
    sdk: flutter

  # Localization
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # Riverpod (State Management + DI)
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Data Classes
  freezed_annotation: ^3.1.0

  # Router
  go_router: ^17.0.1

  # Database
  drift: ^2.28.0
  drift_flutter: ^0.2.4
  path_provider: ^2.1.6

  # Window
  window_manager: ^0.5.0

  # macOS system bridge (screens, launching, window control, menu bar, login item)
  workspace_flow_system:
    path: packages/workspace_flow_system

  # Utils
  collection: ^1.19.0
  clock: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.5.0
  riverpod_generator: ^2.6.5
  riverpod_lint: ^2.6.5
  freezed: ^3.0.0
  drift_dev: ^2.28.0
  mocktail: ^1.0.4
  fake_async: ^1.3.3

flutter:
  generate: true
  uses-material-design: true

  fonts:
    - family: JetBrains Mono
      fonts:
        - asset: assets/fonts/JetBrainsMono.ttf
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter.ttf
